<%# Редактирование объявления %>
<h1><%= t('views.ads.edit.edit_ad') %></h1>
<div class="row">
  <div class="buffer-top">
    <%= render 'form' %>
    <%= link_to t('views.ads.form.back'), ad_path(@ad) %>
  </div>
</div>
